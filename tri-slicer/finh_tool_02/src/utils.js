
/* ─ helpers ─ */
export const H      = s => s*Math.sqrt(3)/2;
export const setL   = (el,x1,y1,x2,y2)=>{ el.setAttribute('x1',x1); el.setAttribute('y1',y1);
                                    el.setAttribute('x2',x2); el.setAttribute('y2',y2); };